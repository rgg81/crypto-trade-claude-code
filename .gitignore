# Runtime state & secrets — never commit
state/
memory/        # desk's episodic journal/lessons — runtime state, regenerated per run
.env
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp
